;; (setq dired-listing-switches "-alh")
;; (setq dired-listing-switches "-lhtar --group-directories-first")
;; (setq dired-listing-switches "-lhta --group-directories-first")
;; (setq dired-listing-switches "-lhta --group-directories-first")

;; (setq dired-listing-switches "-aBhl --group-directories-first")
;; (setq dired-listing-switches "-aBhl --group-directories-first")
;; (setq dired-listing-switches "-aBhlr --group-directories-first")
(setq dired-listing-switches "-aBhl --group-directories-first")

;; (setq dired-listing-switches "-lXGh --group-directories-first")

;; (setq dired-listing-switches "")

(require 'dired-sort)
