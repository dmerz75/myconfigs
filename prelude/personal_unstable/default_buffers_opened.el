;; default_buffers_opened.el

(defun open_default_dired_directories ()
  "Open default dired directories of my choosing."
  (interactive)
  (let (( newbuf (generate-new-buffer "sop_dev")))


    )
  )
